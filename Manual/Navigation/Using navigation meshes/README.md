# Using navigation meshes

Uma *navigation mesh* (malha de navegação) é a região por onde o agente pode navegar. Isso é diferente da `NavigationRegion` que é o node representando esta região. Podemos dizer que *navigation mesh* é o objeto abstrato/teórico.